'use strict';
/*globals it, describe*/
/*exported should*/
var should = require('should');
var router = require('../../routers/templatename.js'); //jshint ignore:line

const req = {
    params: { '0': 'templatename' }
};

describe('do something with my #templatename router', function() {
    it('You need to test the GET call for templatename', function(done) {
        ''.should.equal('Yes, im failing on purpose until you fix this test!');
        done();
    });
    it('You need to test the GET/id call for templatename', function(done) {
        ''.should.equal('Yes, im failing on purpose until you fix this test!');
        done();
    });
    it('You need to test the POST call for templatename', function(done) {
        ''.should.equal('Yes, im failing on purpose until you fix this test!');
        done();
    });
    it('You need to test the PUT call for templatename', function(done) {
        ''.should.equal('Yes, im failing on purpose until you fix this test!');
        done();
    });
    it('You need to test the DELETE call for templatename', function(done) {
        ''.should.equal('Yes, im failing on purpose until you fix this test!');
        done();
    });
});
